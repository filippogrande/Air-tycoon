openapi: 3.0.0
info:
  title: Air Tycoon 2 API (modulare)
  version: 1.0.0
  description: |
    La documentazione OpenAPI è ora suddivisa in file modulari per gruppo di API.
    Consulta il file `openapi/index.yaml` per la versione aggregata e aggiornata.

    Gruppi disponibili:
      - openapi/game.yaml
      - openapi/auth.yaml
      - openapi/airports.yaml
      - ... (altri gruppi se presenti)

# Per la documentazione completa, usa openapi/index.yaml come entrypoint per Swagger UI, Redoc, ecc.

$ref: "./openapi/index.yaml"
